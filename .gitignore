# Python
__pycache__/
*.py[cod]
.venv/
venv/
ENV/
*.egg-info/
dist/
build/

# OS / editors
.DS_Store
.vscode/
.idea/

cat > .gitignore << 'EOF'
# Python
__pycache__/
*.py[cod]
.venv/
venv/
ENV/
*.egg-info/
dist/
build/

# OS / editors
.DS_Store
.vscode/
.idea/

# Certs & secrets (à adapter selon ton choix)
certs/*.key
certs/*.pem
certs/*.p12
*.key
*.env

# Captures / rapports générés (si tu veux les garder hors git)
captures/
rapport/*.pdf
EOF
# Certs & secrets (à adapter selon ton choix)
certs/*.key
certs/*.pem
certs/*.p12
*.key
*.env
cat > .gitignore << 'EOF'
# Python
__pycache__/
*.py[cod]
.venv/
venv/
ENV/
*.egg-info/
dist/
build/

# OS / editors
.DS_Store
.vscode/
.idea/

# Certs & secrets (à adapter selon ton choix)
certs/*.key
certs/*.pem
certs/*.p12
*.key
*.env

# Captures / rapports générés (si tu veux les garder hors git)
captures/
rapport/*.pdf
EOF

# Captures / rapports générés (si tu veux les garder hors git)
captures/
rapport/*.pdf
